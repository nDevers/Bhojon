import React from 'react';
import { Outlet } from 'react-router-dom';

const ManageOrder = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default ManageOrder;