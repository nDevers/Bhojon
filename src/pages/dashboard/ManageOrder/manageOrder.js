import React from 'react';
import { Outlet } from 'react-router-dom';

const ManageOrder = () => {
    return (
        <div>
            <h1>Manage Order</h1>
        </div>
    );
};

export default ManageOrder;