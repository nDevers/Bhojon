import React from 'react';
import Pagination from '../../../components/Pagination';

const UserList = () => {
    return (
        <div>

            <Pagination />
        </div>
    );
};

export default UserList;