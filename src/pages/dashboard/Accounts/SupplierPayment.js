import React from "react";
import useWebsiteTitle from "../../../hooks/useWebsiteTitle";

const SupplierPayment = () => {
  // set website title
  useWebsiteTitle('Account | Supplier Payment')

  return <div>Supplier Payment</div>;
};

export default SupplierPayment;
