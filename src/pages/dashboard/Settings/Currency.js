import React from "react";
import useWebsiteTitle from "../../../hooks/useWebsiteTitle";

const Currency = () => {
  // set website title
  useWebsiteTitle('Settings | Currency')

  return <div>Currency</div>;
};

export default Currency;
