import React from "react";
import useWebsiteTitle from "../../../hooks/useWebsiteTitle";

const Language = () => {
  // set website title
  useWebsiteTitle('Settings | Language')

  return <div>Language</div>;
};

export default Language;
